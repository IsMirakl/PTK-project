export interface ProfileResponseDTO {
    fullName: string;
    summary: string;
    handle: string;
    avatarUrl: string;
}

export interface ProfileUpdateDTO {
    fullname: string;
    summary: string;
    handle: string;
}